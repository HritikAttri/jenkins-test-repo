pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo ${WEBHOOK_EVENT_DETAILS}'
                sh 'echo hello world'
            }
        }
    }
}
